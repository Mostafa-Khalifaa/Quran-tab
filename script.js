import { clickLabelEl, observelEl } from './utils';
const labelEl = document.querySelectorAll('label');

const sectionEl = document.querySelectorAll("section");

labelEl.forEach(clickLabelEl);
observelEl(sectionEl);
 